// // make a function return true value from array
// const arr1 = [true, false, true, false, false, true];

// function countTrue(arr) {
//   let count = 0;
//   arr.filter((item) => (item === true ? count++ : count));
//   return count;
// }

// // Write a function redundant that takes in a string str and returns a function that returns str.

// function redundant(str) {
//   return `"${str}"`;
// }

// const str = redundant("hello");

// // find truthy value in arr
// const values = [1, 2, 3, null, false, 4, 5, "", "test", 6, 7];

// const result1 = values.reduce((acc, curr, index) => {
//   index === 0?
//   if (curr) {
//     acc = acc + curr.toString() + ", ";
//   }
//   return acc;
// });

// console.log(result1);
